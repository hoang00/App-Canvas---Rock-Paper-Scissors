class Solution:
    def mergeAlternately(self, word1: str, word2: str) -> str:
        word1 = "abc"
        word2 = "defxyz"
        final_word = ""
        diff = abs(len(word1) - len(word2))
# compare the length between the two words
        if len(word1) > len(word2)


